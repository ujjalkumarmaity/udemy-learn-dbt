
version: 2

models:
  - name: my_first_dbt_model
    description: "A starter dbt model"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null

  - name: my_second_dbt_model
    description: "A starter dbt model"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null

  - name: firdt_dbt_transformation
    description: "A starter firdt_dbt_transformation model"
    columns:
      - name: category
        description: "The primary key for this table"
        tests:
          - unique
          - not_null

  - name: cumulative_orders_by_date
    description: "A starter cumulative_orders_by_date model"
    columns:
      - name: ORDER_DATE
        description: "The primary key for this table"
        tests:
          - unique
          - not_null